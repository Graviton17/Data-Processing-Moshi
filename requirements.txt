# dataprep requirements
# Install everything:        python -m pip install -r requirements.txt
# Pure-logic / tests only:   python -m pip install -r requirements-core.txt
#
# Python: >=3.10 required (pyannote.audio 4.x hard requirement)
# Python: >=3.12 required for numpy>=2.5; use numpy<2.5 if on 3.10/3.11
#
# Last verified: 2026-06-27

# --- Core (always required) ---
numpy>=2.4,<2.6           # 2.5.0 released 2026-06-21; requires Python>=3.12
                           # use numpy>=1.24,<2.5 if on Python 3.10/3.11
pyyaml>=6.0.3              # 6.0.3 latest (Sep 2025), Python>=3.8

# --- Audio I/O & DSP ---
# sphn 0.2.0 released Apr 2025 — requires cmake<4 pre-installed:
#   pip install 'cmake<4' && CMAKE=$(which cmake) pip install sphn==0.2.0
sphn==0.2.0                # updated from 0.1.12; api-compatible with moshi
soundfile>=0.14.0          # 0.14.0 released 2026-06-06; dropped Python<=3.9
pyloudnorm>=0.2.0          # 0.2.0 latest

# --- ML stages (heavy; need a CUDA-capable env) ---
# OPTION A (recommended): keep moshi-finetune alignment
torch==2.6
torchaudio==2.6.0

# OPTION B: latest PyTorch stack (verify moshi-finetune compat first)
# pip install torch==2.11.0 torchaudio==2.11.0 \
#   --index-url https://download.pytorch.org/whl/cu128

# demucs: original Meta repo unmaintained; use inference-only fork
# Drop-in replacement for htdemucs separation, PyTorch 2.x compatible
demucs-infer>=4.1.2        # maintained fork; replaces demucs>=4.0

pyannote.audio>=4.0.5      # 4.0.5 latest (2026-06-22); Python>=3.10 required
                            # Breaking change vs 3.x: new model IDs on HuggingFace
                            # Accept pyannote/speaker-diarization-community-1 license

whisperx>=3.5.0             # 3.5.0 latest (2026-03-10)
                            # WARNING: do NOT pin any 3.1.x — those are yanked

# --- Dev / tests ---
pytest>=9.0                 # 9.0.2 latest (2026-04-07)